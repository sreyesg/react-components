const  founderListItem = (props) => {
    return (
        <>
        <li>
            <h3>Hello ${props.name} the ${props.title}</h3>
        </li>
        </>
    )
}

export default founderListItem